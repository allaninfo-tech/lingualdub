"""
Component implementations.

This package organises built-in component interfaces by task category.
Each sub-package defines the base interface for its task type. Concrete
model implementations are registered through the Registry and do not
need to live here.
"""
